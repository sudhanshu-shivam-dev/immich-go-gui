name: Build and Release Apps

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version for test build (e.g., 0.9.5-rc1)'
        required: false
        default: '1.0.0-test'
  push:
    tags:
      - 'v*'

env:
  CREATE_DMG_VERSION: '8.1.0'
  APPIMAGETOOL_VERSION: '1.9.1'
  NFPM_VERSION: '2.47.0'

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Setup uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0

      - name: Install dependencies
        run: uv sync --dev

      - name: Install Linux Desktop Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-x11-0 libxcb-cursor0 xvfb libegl1 libgl1 libxcb-xinerama0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-render-util0 libxcb-shape0

      - name: Run pytest
        env:
          QT_QPA_PLATFORM: offscreen
        run: xvfb-run uv run pytest

      - name: Self-test entrypoint
        run: uv run python app.py --self-test

  build:
    needs: test
    runs-on: ${{ matrix.os }}
    outputs:
      version: ${{ steps.set-version.outputs.version }}
    env:
      VERSION: '1.0.0'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            name: linux
            artifact: linux
          - os: windows-latest
            name: windows
            artifact: windows
          - os: macos-latest
            name: macos
            arch: arm64
            artifact: macos-arm64
          - os: macos-15-intel
            name: macos
            arch: x86_64
            artifact: macos-x86_64

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Set Version
        id: set-version
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" && -n "${{ github.event.inputs.version }}" ]]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF_NAME#v}
            if [[ "$VERSION" == *"/"* ]] || [[ -z "$VERSION" ]]; then VERSION="1.0.0-test"; fi
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Setup uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0

      - name: Install dependencies
        run: uv sync --dev

      - name: Detect macOS architecture
        if: matrix.name == 'macos'
        id: macos-arch
        run: |
          ARCH=$(uname -m)
          if [[ "$ARCH" == "arm64" ]]; then
            echo "suffix=macOS-arm64" >> $GITHUB_OUTPUT
          else
            echo "suffix=macOS-x86_64" >> $GITHUB_OUTPUT
          fi

      - name: Build Windows
        if: matrix.name == 'windows'
        shell: bash
        run: |
          NUMERIC_VERSION=$(echo "${{ env.VERSION }}" | grep -oE '^[0-9]+\.[0-9]+\.[0-9]+' || echo "1.0.0")
          uv run python -m nuitka --standalone --enable-plugin=pyside6 --output-filename=Immich-Go-GUI.exe \
            --include-data-files=immich-go-gui.png=immich-go-gui.png \
            --include-data-files=core/flags.toml=core/flags.toml \
            --include-data-dir=assets=assets \
            --include-data-dir=core/fixtures=core/fixtures \
            --windows-console-mode=disable --windows-icon-from-ico=immich-go-gui.ico \
            --company-name="Shitan198u" --product-name="Immich-Go GUI" \
            --file-version="${NUMERIC_VERSION}.0" --product-version="${NUMERIC_VERSION}.0" app.py

      - name: Create Windows Portable Archive
        if: matrix.name == 'windows'
        run: |
          if (Test-Path "app.dist/app.exe") { Rename-Item -Path "app.dist/app.exe" -NewName "Immich-Go-GUI.exe" }
          Set-Content -Path "app.dist/README.txt" -Value "To launch the application, simply double-click the file named Immich-Go-GUI.exe. You do not need to install anything else."
          Compress-Archive -Path "app.dist/*" -DestinationPath "Immich-Go-GUI-${{ env.VERSION }}-Windows-x86_64-Portable.zip"

      - name: Compile Windows Installer
        if: matrix.name == 'windows'
        shell: powershell
        run: |
          if (-not (Test-Path "C:\Program Files (x86)\Inno Setup 6\ISCC.exe")) {
            choco install innosetup -y
          }
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" /DMyAppVersion="${{ env.VERSION }}" /DMyOutputFilename="Immich-Go-GUI-${{ env.VERSION }}-Windows-x86_64-Setup" packaging/windows/installer.iss
          if (Test-Path "..\..\Immich-Go-GUI-*.exe") {
            Move-Item -Path "..\..\Immich-Go-GUI-*.exe" -Destination "." -Force
          }
          if (Test-Path "..\Immich-Go-GUI-*.exe") {
            Move-Item -Path "..\Immich-Go-GUI-*.exe" -Destination "." -Force
          }

      - name: Setup Node.js
        if: matrix.name == 'macos'
        uses: actions/setup-node@v7
        with:
          node-version: '20'

      - name: Build macOS
        if: matrix.name == 'macos'
        run: |
          uv run python -m nuitka --standalone --enable-plugin=pyside6 --output-filename=Immich-Go-GUI \
            --include-data-files=immich-go-gui.png=immich-go-gui.png \
            --include-data-files=core/flags.toml=core/flags.toml \
            --include-data-dir=assets=assets \
            --include-data-dir=core/fixtures=core/fixtures app.py
          npm install -g create-dmg@${{ env.CREATE_DMG_VERSION }}
          create-dmg app.app
          mv *.dmg "Immich-Go-GUI-${{ env.VERSION }}-${{ steps.macos-arch.outputs.suffix }}.dmg"

      - name: Build Linux Standalone
        if: matrix.name == 'linux'
        run: |
          uv run python -m nuitka --standalone --enable-plugin=pyside6 --output-filename=Immich-Go-GUI \
            --include-data-files=immich-go-gui.png=immich-go-gui.png \
            --include-data-files=core/flags.toml=core/flags.toml \
            --include-data-dir=assets=assets \
            --include-data-dir=core/fixtures=core/fixtures app.py

      - name: Create Linux Portable Archive
        if: matrix.name == 'linux'
        run: |
          if [ -f app.dist/app.bin ]; then mv app.dist/app.bin app.dist/Immich-Go-GUI; fi
          if [ -f app.dist/app ]; then mv app.dist/app app.dist/Immich-Go-GUI; fi
          echo "To launch the application, double-click the file named Immich-Go-GUI, or run ./Immich-Go-GUI in the terminal. You do not need to install anything else." > app.dist/README.txt
          tar -czvf "Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64-Portable.tar.gz" -C app.dist .

      - name: Build Linux AppImage
        if: matrix.name == 'linux'
        run: |
          mkdir -p AppDir/usr/bin
          cp -r app.dist/* AppDir/usr/bin/
          cp immich-go-gui.png AppDir/

          cat << 'EOF' > AppDir/immich-go-gui.desktop
          [Desktop Entry]
          Name=Immich-Go GUI
          Exec=Immich-Go-GUI
          Icon=immich-go-gui
          Type=Application
          Categories=Utility;
          EOF

          cat << 'EOF' > AppDir/AppRun
          #!/bin/sh
          HERE="$(dirname "$(readlink -f "${0}")")"
          exec "${HERE}/usr/bin/Immich-Go-GUI" "$@"
          EOF
          chmod +x AppDir/AppRun

          wget -q "https://github.com/AppImage/appimagetool/releases/download/${{ env.APPIMAGETOOL_VERSION }}/appimagetool-x86_64.AppImage" -O appimagetool
          chmod +x appimagetool
          ./appimagetool --appimage-extract-and-run AppDir "Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.AppImage"

      - name: Install nFPM
        if: matrix.name == 'linux'
        run: |
          curl -sL "https://github.com/goreleaser/nfpm/releases/download/v${NFPM_VERSION}/nfpm_${NFPM_VERSION}_Linux_x86_64.tar.gz" | tar xz nfpm
          sudo mv nfpm /usr/local/bin/

      - name: Build DEB and RPM
        if: matrix.name == 'linux'
        run: |
          cd packaging/linux
          nfpm pkg --packager deb --target "../../Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.deb"
          nfpm pkg --packager rpm --target "../../Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.rpm"

      - name: Generate SHA256 checksums
        shell: bash
        run: |
          SUMS_FILE="Immich-Go-GUI-${{ env.VERSION }}-SHA256SUMS.txt"
          : > "$SUMS_FILE"
          for f in Immich-Go-GUI-${{ env.VERSION }}-*; do
            if [ -f "$f" ]; then
              sha256sum "$f" >> "$SUMS_FILE"
            fi
          done

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Immich-Go-GUI-${{ matrix.artifact }}
          if-no-files-found: error
          path: |
            Immich-Go-GUI-${{ env.VERSION }}-Windows-x86_64-Setup.exe
            Immich-Go-GUI-${{ env.VERSION }}-Windows-x86_64-Portable.zip
            Immich-Go-GUI-${{ env.VERSION }}-macOS-*.dmg
            Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.AppImage
            Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64-Portable.tar.gz
            Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.deb
            Immich-Go-GUI-${{ env.VERSION }}-Linux-x86_64.rpm
            Immich-Go-GUI-${{ env.VERSION }}-SHA256SUMS.txt

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Official Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: |
            artifacts/Immich-Go-GUI-*-Windows-x86_64-Setup.exe
            artifacts/Immich-Go-GUI-*-Windows-x86_64-Portable.zip
            artifacts/Immich-Go-GUI-*-macOS-*.dmg
            artifacts/Immich-Go-GUI-*-Linux-x86_64.AppImage
            artifacts/Immich-Go-GUI-*-Linux-x86_64-Portable.tar.gz
            artifacts/Immich-Go-GUI-*-Linux-x86_64.deb
            artifacts/Immich-Go-GUI-*-Linux-x86_64.rpm
            artifacts/Immich-Go-GUI-*-SHA256SUMS.txt

      - name: Create Test Pre-Release
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "test-${{ needs.build.outputs.version }}"
          name: "Test Build test-${{ needs.build.outputs.version }}"
          prerelease: true
          files: |
            artifacts/Immich-Go-GUI-*-Windows-x86_64-Setup.exe
            artifacts/Immich-Go-GUI-*-Windows-x86_64-Portable.zip
            artifacts/Immich-Go-GUI-*-macOS-*.dmg
            artifacts/Immich-Go-GUI-*-Linux-x86_64.AppImage
            artifacts/Immich-Go-GUI-*-Linux-x86_64-Portable.tar.gz
            artifacts/Immich-Go-GUI-*-Linux-x86_64.deb
            artifacts/Immich-Go-GUI-*-Linux-x86_64.rpm
            artifacts/Immich-Go-GUI-*-SHA256SUMS.txt
